# Everything: the package with all optional dependencies (maps, scraping, dev).
# For the exact versions the results were produced with: pip install -r requirements-lock.txt && pip install -e . --no-deps
-e .[all]
